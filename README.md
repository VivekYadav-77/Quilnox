# Smart Leads Dashboard

Full-stack MERN lead management dashboard built with TypeScript.

## Phase 1

- `server`: Express + TypeScript API scaffold
- `client`: React + Vite + TypeScript + Tailwind scaffold
- Docker base files for client, server, and MongoDB

## Scripts

Run each service from its own folder:

```bash
npm run dev
npm run build
npm run lint
```
